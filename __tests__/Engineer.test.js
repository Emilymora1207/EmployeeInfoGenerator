const Engineer = require('../lib/Engineer');

describe('Employee', () => {
    describe('getGithub', () => {
        it('should get the GitHub username of the Engineer', () => {
            const employeeGitHub = new getGithub();
            const GitHub = 'Emilymora1207';

            expect(employeeGitHub) = GitHub;
        })
    })
    describe('getRole', () => {
        it('should get the Id of hte Engineer', () => {
            const gettingRole = new getRole();

            expect(gettingRole) = Engineer;
        })
    })
})